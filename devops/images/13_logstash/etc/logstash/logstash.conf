input {
	lumberjack {
	    port => 5043
	    ssl_certificate => '/etc/ssl/logstash/logstash.crt'
	    ssl_key => '/etc/ssl/logstash/logstash.key'
	}
}
output {
	elasticsearch {
		host => "elasticsearch"
		cluster => "logstash"
	}
}