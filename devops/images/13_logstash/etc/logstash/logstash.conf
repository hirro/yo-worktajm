input {
	lumberjack {
	    port => 5043
	    ssl_certificate => '/etc/ssl/server.crt'
	    ssl_key => '/etc/ssl/server.key'
	}
}
output {
	elasticsearch {
		host => "elasticsearch"
		cluster => "logstash"
	}
}