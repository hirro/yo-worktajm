{
    "network": {
        "servers": [ "logstash:5043" ],
        "ssl certificate": "/etc/ssl/logstash/logstash.crt",
        "ssl key": "/etc/ssl/logstash/logstash.key",
        "ssl ca": "/etc/ssl/logstash/logstash.crt"
    },
    "files": [
        {
            "paths":  [ "/var/log/nginx/worktajm-access.log" ],
            "fields": { "type": "nginx-access" }
        }
    ]
}