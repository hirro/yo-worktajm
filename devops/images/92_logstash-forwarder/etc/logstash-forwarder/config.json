{
    "network": {
        "servers": [ "logstash:5043" ],
        "ssl ca": "/etc/logstash-forwarder/logstash.crt"
    },
    "files": [
        {
            "paths":  [ "/var/log/nginx/worktajm-access.log" ],
            "fields": { "type": "nginx-access" }
        }
    ]
}